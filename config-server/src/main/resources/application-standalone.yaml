server:
  port: 8888
spring:
  application:
    name: selmag-config-server
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/projects/study-projects/selmag-parent/config/cloud/{application}/profile